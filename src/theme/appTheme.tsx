import { StyleSheet } from "react-native";

export const styleGlobal=StyleSheet.create({
    container:{
        flex:1,
        alignItems: 'center',
        justifyContent: 'center',
        gap: 20
      },
    
      tilte:{
        fontSize: 15,
        fontWeight: 'bold'
      }
})